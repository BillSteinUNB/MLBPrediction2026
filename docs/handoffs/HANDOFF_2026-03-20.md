# Handoff 2026-03-20

This handoff is for resuming work on the MLB F5 project from another machine.

## Current Goal

The immediate goal is not new model ideas. It is to get the real walk-forward training/backtest path stable enough to:

1. Train on pre-2024 data and evaluate on 2024.
2. Retrain through 2024 and evaluate on 2025.
3. Use those honest holdouts to decide whether the baseline model is good enough for 2026 prep.

## What Was Completed

### Blocking fixes

1. Real training tests now point at actual cached parquet when available instead of synthetic fixtures.
2. CLV tracking is bookmaker-aware.
3. Retry and circuit breaker composition was corrected so the breaker sees raw failures and `CircuitBreakerOpenError` is not retried.

### Automation validation

The pending automation assertions were validated locally with targeted commands:

- scheduler CLI validation
- env validation failure-path checks
- retry/exponential backoff evidence
- circuit breaker activation evidence
- log rotation validation

### Real backtest-path fixes

While attempting a real walk-forward run, several runtime issues were found and fixed:

1. `pybaseball.team_game_logs()` pandas-3 compatibility issue in `src/clients/statcast_client.py`
2. Empty pitching-history datetime dtype issue in `src/features/pitching.py`
3. Nullable ratio handling issue in `src/features/defense.py`
4. Empty bullpen-history datetime dtype issue in `src/features/bullpen.py`

### Progress visibility

Readable progress logging was added to:

- `src/backtest/walk_forward.py`
- `src/model/data_builder.py`

This is phase logging, not a live animated progress bar.

## Files Changed

- `src/backtest/walk_forward.py`
- `src/clients/odds_client.py`
- `src/clients/statcast_client.py`
- `src/db.py`
- `src/engine/edge_calculator.py`
- `src/features/bullpen.py`
- `src/features/defense.py`
- `src/features/pitching.py`
- `src/model/data_builder.py`
- `src/models/bet.py`
- `src/ops/error_handler.py`
- `src/ops/performance_tracker.py`
- `src/pipeline/daily.py`
- `tests/model/test_antileak.py`
- `tests/features/test_bullpen_features.py`
- `tests/ops/test_db_schema.py`
- `tests/ops/test_error_handler.py`
- `tests/ops/test_performance_tracker.py`
- `tests/pipeline/test_data_integrity.py`
- `tests/pipeline/test_pipeline_error_handling.py`
- `tests/integration/test_statcast_client.py`
- `.gitignore`

## Known Current State

- The daily inference CLI works.
- The true training/backtest path now gets much farther than before.
- Real raw cache files were being created under `data/raw/` during the walk-forward probe.
- No full feature-rich training parquet exists yet in `data/training/`; the prior available parquet was only a validation fixture and not enough to train the real model.
- Generated caches and logs should stay local and are now ignored by git.

## Last User Run

The latest command run manually was:

```powershell
python -m src.backtest.walk_forward `
  --start 2024-04-01 `
  --end 2024-04-01 `
  --output-dir temp_backtest_probe
```

Before it was interrupted, it showed:

- backtest start
- 1 requested window
- train window `2023-10-01..2024-03-31`
- test window `2024-04-01..2024-04-30`
- training-data build start
- schedule fetches for 2023 and 2024
- cutoff `< 2024-05-01T00:00:00+00:00`
- `2882` scheduled games
- progress reached `[build 1/219] ... bullpen`

The user stopped the run because they were moving to a faster home PC.

## Recommended Next Step On The Home PC

1. Clone or pull this branch.
2. Set up `.env` with rotated API keys.
3. Run the same one-window probe again:

```powershell
python -m src.backtest.walk_forward `
  --start 2024-04-01 `
  --end 2024-04-01 `
  --output-dir temp_backtest_probe
```

4. If it fails, capture the full traceback and fix the next runtime blocker.
5. If it succeeds, inspect the generated metrics/artifacts and then extend to a longer range.

## Suggested Follow-up Sequence

After the one-window probe succeeds:

1. Expand to a larger 2024 range.
2. Build the real training parquet/artifacts needed for model fitting.
3. Run the honest holdout protocol:
   - train through 2023, test 2024
   - train through 2024, test 2025
4. Evaluate Brier, log loss, ECE/calibration, bet count, CLV, ROI, drawdown.

## Prompt To Resume In Another AI Chat

Use this as the first message in the new chat:

```text
I am resuming work on the MLBPrediction2026 repo. Read `docs/handoffs/HANDOFF_2026-03-20.md` first, then inspect git status and continue from there.

Context:
- The three original blocking fixes were already completed:
  1. real training tests use actual cached parquet when available
  2. bookmaker identity now flows through CLV tracking
  3. retry/circuit-breaker composition was fixed
- Additional runtime fixes were already made for real walk-forward execution:
  1. pybaseball team_game_logs pandas-3 compatibility fallback
  2. empty pitching-history datetime dtype fix
  3. defense nullable ratio fix
  4. empty bullpen-history datetime dtype fix
- Progress logging was added to walk_forward and data_builder.
- Generated caches/logs should not be committed.

Immediate task:
- Run and stabilize:
  python -m src.backtest.walk_forward --start 2024-04-01 --end 2024-04-01 --output-dir temp_backtest_probe
- If it fails, fix the next real runtime blocker.
- If it succeeds, summarize the outputs and propose the next walk-forward command.

User goal:
- Build a proper, leak-free MLB F5 prediction model.
- Evaluate honestly on 2024 and then 2025 before preparing for the 2026 season.
- Ignore autoresearch/agentic self-improvement for now.
```

## Notes

- The user pasted API keys in chat during setup. Those should be rotated before serious use.
- The home PC is expected to be much faster than the laptop for these long cache/build runs.
