Legacy collectors live here.

Current status:
- `sbr_legacy.py`: former primary SportsbookReview scraper and shared SQLite store.

Intent:
- keep the SBR path runnable as a fallback
- keep new acquisition work, especially OddsPortal, out of the legacy file
- only remove this path after the new source is proven against real overlap
