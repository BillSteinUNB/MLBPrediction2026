- 2026-03-21: Dashboard adapters should consume `build_experiment_metrics_dataframe()` output directly and only enrich with JSON detail fields (feature importance, reliability bins, quality gates) to avoid reimplementing lane delta logic.
- 2026-03-21: Lanes endpoint complete and tested. GET /api/lanes returns list[Lane] with lane grouping by (holdout_season, target_column, variant). Each lane contains lane_id, model_name, variant, best_run (by metrics priority: ROC-AUC > accuracy > log-loss > brier), and latest_run (by timestamp). Router properly registered in main.py. All 40 dashboard tests pass.
